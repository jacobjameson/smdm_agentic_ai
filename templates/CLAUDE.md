# Project: <PROJECT NAME>

<!--
  This is a CLAUDE.md template. Copy it to the root of your project as
  CLAUDE.md. Claude Code reads it automatically at the start of every
  session. Delete the guidance comments (like this one) once you've
  filled it in. Keep it short — durable facts and preferences only.
-->

## What this repo does
<!-- One paragraph: the research question and what the code produces. -->
<e.g., Estimates the cost-effectiveness of screening strategy X vs. Y using
a Markov cohort model. Produces the main results table and figures for the
manuscript.>

## Structure
<!-- What lives where. Adjust to your project. -->
- `data/raw/`    — source data; **never edit**
- `data/clean/`  — generated by scripts in `R/` (or `src/`)
- `R/`           — numbered pipeline: `01_clean.R`, `02_model.R`, `03_figs.R`
- `figs/`        — output figures
- `tables/`      — output tables
- `.claude/skills/` — project skills

## How to run
<!-- The single command/script that reproduces everything from raw data. -->
`source("R/00_run_all.R")` reproduces all outputs from `data/raw/`.

## Conventions
- Language: <R (tidyverse) / Python (pandas)>. Readable over clever.
- Style: snake_case; one job per function; comment the *why*, not the *what*.
- Figures: colorblind-safe palette; labelled axes; 300 dpi; save to `figs/`.
- Small, testable steps over big rewrites.

## Guardrails
- Never modify anything in `data/raw/`.
- Never run destructive git commands (`reset --hard`, force push, `clean`) without asking.
- Don't install new packages without asking.
- Make changes on a branch; show me the diff before committing.
- Never fabricate data values, parameters, or citations — read them from the source.

## TODO / Next steps
<!-- Optional: capture where to pick up next session. -->
- <next thing to do>
